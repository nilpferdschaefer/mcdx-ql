[package]
name = "mcdx_ql"
version = "0.1.0"
edition = "2021"
description = "Compile indicator expression grammar to joint analytics SQL"
license = "MIT"
readme = "README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
pretty_assertions = "1"
